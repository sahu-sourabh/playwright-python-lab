[pytest]
# 'testpaths' tells pytest where to look by default
testpaths = tests
# 'addopts' adds your favorite flags automatically
addopts = --browser chromium --html=reports/report.html --self-contained-html
